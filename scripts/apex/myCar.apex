string carBrand = 'Honda';
system.debug('car model '+ carBrand);

string carModel = 'CRV';
system.debug(carModel);

string carColor = 'White';
system.debug(carColor);

string myCar = carBrand +' ' + carModel + ' ' + carColor;
system.debug(myCar);